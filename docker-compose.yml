version: '3.8'
services:
  postgres:
    image: postgres:16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: orgcodeai
      POSTGRES_PASSWORD: postgres
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'

