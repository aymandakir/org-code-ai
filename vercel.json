{
  "version": 2,
  "buildCommand": "npm install && cd apps/dashboard && npm install --legacy-peer-deps && npm run build -- --webpack",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
