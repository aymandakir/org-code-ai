{
  "version": 2,
  "buildCommand": "npm install && cd apps/dashboard && npm install --legacy-peer-deps && NEXT_PRIVATE_SKIP_TURBO=1 npm run build",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
